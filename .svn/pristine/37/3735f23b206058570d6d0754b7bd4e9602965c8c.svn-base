import React from 'react'  
import Tooltip from '@mui/material/Tooltip'; 
import IconButton from '@mui/material/IconButton';
import { Container, ButtonsContainer, StyledButton, WarningIcon, Typography } from './payment-method.styled'
const PaymentMethod = ({selectedPaymentMethod, setSelectedPaymentMethod}) => {
    return <Container>
        <ButtonsContainer>
            <StyledButton onClick={() => setSelectedPaymentMethod(0)} backgroundColor={selectedPaymentMethod === 0 ? '#6ec4e8' : '#c8c8c8'}><Typography>Nómina</Typography></StyledButton>
            <StyledButton onClick={() => setSelectedPaymentMethod(1)} backgroundColor={selectedPaymentMethod === 1 ? '#6ec4e8' : '#c8c8c8'}><Typography>Efectivo</Typography></StyledButton>
            <StyledButton onClick={() => setSelectedPaymentMethod(2)} backgroundColor={selectedPaymentMethod === 2 ? '#6ec4e8' : '#c8c8c8'}><Typography>Tarjeta</Typography></StyledButton>
        </ButtonsContainer>
        <Tooltip>
            <IconButton>
                <WarningIcon/>
            </IconButton>
        </Tooltip>
    </Container>
}

export default PaymentMethod